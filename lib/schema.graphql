type Course{
    _id: ID!
    title: String!
    teacher: String!
    description: String!
    topic: String
}

type Query {
    "Return all courses"
    getCourses:[Course]
    "Return a specific course"
    getCourse(id:ID!):Course # The '!' sign defines which parameter is required
}
